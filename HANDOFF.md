# HANDOFF.md

บันทึกความคืบหน้า/ไอเดียสำหรับ session ถัดไป (ข้ามเครื่อง บ้าน ↔ ที่ทำงาน) — GitHub คือสะพานซิงค์

---

## ไอเดียถัดไป (ยังไม่เริ่มทำ): Make.com automation ต่อยอดจาก Email Banner (2569-08-22)

**ไอเดียของพี่ A**: ใช้ **Make.com** เป็น workflow automation ต่อจากขั้นตอนปัจจุบันในเว็บแอป — ข้อมูลเข้า Web App แล้ว
กดปุ่ม "ส่งออก Email Banner" → คัดลอกรูปภาพ → เขียนอีเมล → ส่งอีเมล ให้เป็นอัตโนมัติทั้งหมด

**สถานะปัจจุบันของโค้ด (เช็คแล้ว)**: "สร้างสรุปอีเมล" (`handleGenerateEmailClick` ใน `src/main.js`) copy ข้อความผ่าน
`navigator.clipboard.writeText` และ "ส่งออก Email Banner" (`handleCopyBannerImage`) copy รูปผ่าน
`navigator.clipboard.write([ClipboardItem])` — ทั้งคู่จบที่ "คัดลอกแล้วให้ผู้ใช้ไปวางเอง" ยังไม่มี `mailto:` หรือการ
ส่งอีเมลอัตโนมัติใดๆ ในโค้ดตอนนี้

### ความเห็น: ไอเดียนี้เป็นไปได้ไหม?

**ตามที่อธิบายมาตรงๆ — ทำไม่ได้** Make.com (เหมือน Zapier/n8n) เป็น cloud SaaS ที่เชื่อมผ่าน API/webhook เท่านั้น
**ไม่มีทางเข้าไป "คลิกปุ่มในเบราว์เซอร์ของพี่ A" หรืออ่าน clipboard ของเครื่อง local ได้เลย** — เป็นข้อจำกัดด้าน
browser security sandbox ไม่ใช่ปัญหาที่แก้ด้วยโค้ดฝั่งเราได้ ดังนั้น flow "กดปุ่ม → Make.com คัดลอกรูป → เขียนอีเมล →
ส่ง" ต่อกันเป็นเส้นเดียวแบบอัตโนมัติทั้งหมดตามที่อธิบายมา **ทำไม่ได้จริง** — ต้องมีคนกดปุ่ม export ในเบราว์เซอร์เองเสมอ
อย่างน้อย 1 ขั้น

### ทางเลือกที่แนะนำ (เรียงจากง่าย/ถูกที่สุด ไปยาก/แพงขึ้น)

1. **แนะนำเป็นอันดับแรก — เพิ่มลิงก์ `mailto:` เติม subject/body ล่วงหน้า** ต่อจากปุ่มที่มีอยู่แล้ว (ใช้ข้อความจาก
   `generateEmailSummary` เดิม) — ลดขั้นตอน "เขียนอีเมล" เหลือแค่ตรวจแล้วกดส่ง ไม่ต้องเพิ่ม service/ค่าใช้จ่ายใดๆ เลย
   ตรงกับสถาปัตยกรรม local-first/no-backend ของโปรเจกต์นี้อยู่แล้ว
2. **ถ้าอยากให้ Make.com ช่วยจริง** — ใช้ Make.com เฉพาะจุดที่มันถนัด คือ "watch โฟลเดอร์ (Google Drive/OneDrive) ที่
   พี่ A เซฟรูป export ไปวาง → ประกอบ+ส่งอีเมลอัตโนมัติ" — ยังต้องมีขั้นตอนมือ 1 ครั้ง (export รูปไปวางในโฟลเดอร์) แต่
   ตัดขั้นตอนเขียน/ส่งอีเมลออกได้จริง
3. **ถ้าอยากได้แบบไม่ต้องกดอะไรเลยจริงๆ (zero-click)** — ต้องมี backend/serverless จริงๆ (เช่น Cloudflare Workers
   ที่โปรเจกต์นี้พิจารณาไว้อยู่แล้วผ่าน `cloudflare-workers-deploy` skill) รับข้อมูลสรุปรายเดือน (จากจุดเดียวกับที่
   export CSV อยู่แล้ว) แล้วเรียก email-sending API (เช่น Resend/SendGrid) ส่งเอง — ตัด Make.com ออกไปเลยได้ เพราะทำเอง
   บน Cloudflare Workers free tier ก็พอ ไม่ต้องเสียค่า subscription Make.com สำหรับงานที่ backend เล็กๆ ทำเองได้

**อัปเดต (2569-08-22 เย็น)**: พี่ A เลือกทางเลือกที่ 1 แล้ว — **"เพิ่ม mailto: prefill ให้เลย"** — หนูอธิบายให้ฟังก่อนว่า
`mailto:` คือลิงก์พิเศษที่สั่งเปิดโปรแกรมอีเมล default ของเครื่อง พร้อมกรอก subject/body ให้ล่วงหน้า (ผ่าน query string
เช่น `mailto:?subject=...&body=...`) — **ข้อจำกัดสำคัญ: แนบรูปภาพไม่ได้** (เป็นข้อจำกัดของมาตรฐาน mailto: เอง) ดังนั้น
จะช่วยตัดแค่ขั้นตอน "เขียนอีเมล" ส่วน "Email Banner" (รูปภาพ) พี่ A ยังต้อง copy-paste รูปเองเหมือนเดิม — พี่ A รับทราบและ
ขอให้เริ่มทำ **พรุ่งนี้**

### งานที่รอทำต่อ (พรุ่งนี้): เพิ่มปุ่ม/ลิงก์ mailto: prefill

**ขอบเขต**: แก้ที่ปุ่ม "สร้างสรุปอีเมล" (`handleGenerateEmailClick` ใน `src/main.js`, เรียก
`UI_RENDERER.generateEmailSummary(...)` เพื่อสร้างข้อความอยู่แล้ว) — เพิ่มทางเลือกใหม่ให้เปิด
`mailto:?subject=...&body=...` (encode ด้วย `encodeURIComponent`) แทน/คู่กับปุ่ม copy ข้อความเดิม (ปุ่ม copy เดิมน่าจะ
เก็บไว้เป็น fallback เพราะบาง client ตัดข้อความยาวเกิน ~2000 ตัวอักษร) — **ยังไม่ได้ตกลงรายละเอียด UI ว่าจะเป็นปุ่มแยก
หรือเปลี่ยนปุ่มเดิม ต้องคุยกับพี่ A ก่อนเริ่มเขียนโค้ด** ตามกติกา Blueprint → รออนุมัติ
- ไม่กระทบปุ่ม "ส่งออก Email Banner"/`handleCopyBannerImage` เลย (รูปภาพยังคง copy-paste มือเหมือนเดิม)
- Critical file: `src/main.js` (ฟังก์ชัน `handleGenerateEmailClick` และ modal action handler ของมัน)
